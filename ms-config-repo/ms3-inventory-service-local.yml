spring:
  datasource:
    driver-class-name: org.h2.Driver
    data-username: inventory
    data-password: inventory
    url: jdbc:h2:mem:inventorydb

  application:
    name: ms3-inventory-service

server:
  port: ${PORT:62513}

eureka: 
  client:
    serviceUrl: 
      defaultZone: http://${EUREKA_HOST:localhost}:${EUREKA_PORT:62513}/eureka/